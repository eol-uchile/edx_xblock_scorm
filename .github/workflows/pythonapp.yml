name: Python application

on:
  push:
    branches:
      - 'master'
      - 'koa'
      - 'lilac'
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - 'master'
      - 'koa'
      - 'lilac'

jobs:
  build:
    uses: eol-uchile/workflow-commons/.github/workflows/pythonapp.yml@1.0.0
    with:
      app_id: ${{ vars.EOLITO_BOT_APP_ID }}
      django-lms: true
      django-cms: false
      platform-eol: true
      platform-openuchile: true
    secrets:
      private_key: ${{ secrets.EOLITO_BOT_PRIVATE_KEY }}
